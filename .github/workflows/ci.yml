name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  quality-gate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run harness quality gate
        run: bash harness/scripts/dev.sh
