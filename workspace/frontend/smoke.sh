#!/usr/bin/env bash

set -u

printf 'frontend smoke: placeholder only. Replace workspace/frontend/smoke.sh with a fast but complete smoke suite that boots the app shell and covers the full set of primary user journeys when the frontend stack is chosen.\n'
