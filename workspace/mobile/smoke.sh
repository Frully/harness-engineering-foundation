#!/usr/bin/env bash

set -u

printf 'mobile smoke: placeholder only. Replace workspace/mobile/smoke.sh with a fast but complete smoke suite that boots the app, renders the first screen, and covers the full set of primary user journeys when the mobile stack is chosen.\n'
